{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://e2grnd.github.io/json-schemas/webhooks/v1/eec.calculation.failed.schema.json",
  "title": "eec.calculation.failed data",
  "description": "Data payload for the eec.calculation.failed event. Models CalcResult.Error from activitycontracts alongside standard traceability fields.",
  "type": "object",
  "additionalProperties": false,
  "required": ["jobId", "tenantKey", "calcType"],
  "properties": {
    "jobId": {
      "type": "string",
      "format": "uuid",
      "description": "UUIDv7 job identifier; also the Temporal workflow ID."
    },
    "tenantKey": {
      "type": "string",
      "description": "Tenant identifier of the submitting organization."
    },
    "calcType": {
      "type": "string",
      "enum": ["eec"],
      "description": "Calculator type. Always 'eec' for this event."
    },
    "calcName": {
      "type": "string",
      "description": "Optional human-readable calculation name supplied at submission."
    },
    "failedAt": {
      "type": "string",
      "format": "date-time",
      "description": "UTC timestamp when the failure was recorded."
    },
    "errorCode": {
      "type": "string",
      "description": "Machine-readable error code (e.g. 'TIMEOUT', 'WORKER_PANIC', 'JOB_GRPC_ERROR')."
    },
    "errorMessage": {
      "type": "string",
      "description": "Human-readable failure description, sourced from CalcResult.Error."
    }
  }
}
