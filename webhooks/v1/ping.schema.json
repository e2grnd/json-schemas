{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://e2grnd.github.io/json-schemas/webhooks/v1/ping.schema.json",
  "title": "ping data",
  "description": "Data payload for the ping event. A lightweight liveness check with no required fields. Receivers should respond 200 OK. Additional properties are permitted to allow future extensions without a schema version bump.",
  "type": "object",
  "additionalProperties": true,
  "properties": {
    "message": {
      "type": "string",
      "default": "ping",
      "description": "Optional message, defaults to 'ping'."
    }
  }
}
